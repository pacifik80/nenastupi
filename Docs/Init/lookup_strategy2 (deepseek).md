## Стратегия идентификации компании с минимизацией вызовов DeepSeek  
*Каскадный подход: правила → кэш → внешний поиск → LLM (только при необходимости)*

---

### 🎯 Ключевой принцип: «LLM как последнее средство»

**Цель:** Снизить долю запросов, требующих вызова DeepSeek, до **≤5%** от общего трафика.  
**Результат:** При 100 запросах/день → ~5 вызовов к LLM → ~1000 токенов → **бесплатно в рамках квоты** (5 млн токенов/мес).

---

## 📊 Матрица обработки запросов по типу

| Тип ввода | Детекция (без LLM) | Стратегия идентификации | Вызов LLM? | Стоимость |
|-----------|-------------------|-------------------------|------------|-----------|
| **ИНН (10/12 цифр)** | Регулярное выражение `^\d{10}$|^\d{12}$` | 1. Прямой запрос к ФНС по ИНН<br>2. Если не найдено → Контур.Фокус | ❌ Нет | 0 токенов |
| **ОГРН (13/15 цифр)** | Регулярное выражение `^\d{13}$|^\d{15}$` + валидация контрольной суммы | 1. Прямой запрос к ФНС по ОГРН<br>2. Валидация статуса компании | ❌ Нет | 0 токенов |
| **URL сайта** | Паттерн `https?://` или домен `.ru`/`.com` | 1. Извлечение домена<br>2. Поиск в Яндексе: `"{домен} официальный сайт ИНН"`<br>3. Парсинг сниппетов на ИНН<br>4. Если ИНН найден → запрос к ФНС | ⚠️ Только если ИНН не найден в сниппетах | ~150 токенов |
| **Название/бренд** | Любой другой текст | См. каскадную стратегию ниже | ⚠️ Только при неоднозначности | ~200 токенов |

---

## 🔁 Каскадная стратегия для названий/брендов

```mermaid
flowchart TD
    A["Ввод пользователя:<br>'яндкс такси'"] --> B{Этап 1: Классификация типа<br>(правила без LLM)}
    
    B -->|ИНН/ОГРН/URL| C["Обработка по специальному сценарию"]
    B -->|Название/бренд| D["Этап 2: Проверка кэша<br>ключ = хэш(нормализованный_запрос)"]
    
    D -->|Найдено| E["Возврат сохранённого ИНН/ОГРН"]
    D -->|Не найдено| F["Этап 3: Нормализация правилами"]
    
    F --> G["Этап 4: Поиск в ФНС по нормализованному названию"]
    
    G --> H{Результаты поиска}
    
    H -->|1 компания,<br>релевантность >0.85| I["✅ Однозначное совпадение"]
    H -->|0 компаний| J["Этап 5: Внешний поиск:<br>Яндекс 'яндкс такси официальный сайт ИНН'"]
    H -->|2–3 компании,<br>релевантность 0.6–0.85| K["Этап 6: Вызов DeepSeek<br>с контекстом результатов ФНС"]
    H -->|>3 компании или<br>релевантность <0.6| L["Этап 7: Вызов DeepSeek<br>с запросом на уточнение"]
    
    J --> M{Найден ИНН в сниппетах?}
    M -->|Да| N["Переход к обработке по ИНН"]
    M -->|Нет| K
    
    K --> O["DeepSeek возвращает:<br>• Выбранный ИНН ИЛИ<br>• Уточняющий вопрос"]
    
    L --> O
    
    O --> P{Тип ответа}
    P -->|ИНН выбран| Q["Запрос к ФНС по ИНН"]
    P -->|Требуется уточнение| R["Показать пользователю варианты:<br>1. Яндекс.Такси (ИНН 7704431421)<br>2. Яндекс Такси Сервис (ИНН 9701234567)"]
    
    Q --> S["Этап 8: Кэширование результата<br>ключ → ИНН на 24 часа"]
    R --> T["Ожидание выбора пользователя"]
    T --> Q
    
    I --> S
    E --> U["Возврат в основной поток"]
    S --> U
    
    style B fill:#3498db,color:white
    style D fill:#2ecc71,color:white
    style F fill:#2ecc71,color:white
    style G fill:#2ecc71,color:white
    style K fill:#f39c12,color:white
    style L fill:#f39c12,color:white
    style O fill:#f39c12,color:white
    style S fill:#27ae60,color:white
```

---

## 🧠 Детализация этапов без вызова LLM

### Этап 1: Классификация типа запроса (100% правилами)

```python
# Псевдокод логики (реализуется без LLM)
if re.match(r"^\d{10}$", query) and validate_inn_10(query):
    type = "inn_legal"
elif re.match(r"^\d{12}$", query) and validate_inn_12(query):
    type = "inn_ip"
elif re.match(r"^\d{13}$", query) and validate_ogrn(query):
    type = "ogrn"
elif re.match(r"^\d{15}$", query) and validate_ogrnip(query):
    type = "ogrnip"
elif re.match(r"https?://", query) or re.search(r"\.(ru|com|рф)$", query):
    type = "url"
else:
    type = "name"
```

### Этап 2: Кэширование нормализованных запросов

| Ключ кэша | Значение | Срок жизни |
|-----------|----------|------------|
| `normalize:яндкс такси` | `яндекс такси` | 7 дней |
| `resolve:яндекс такси` | `ИНН 7704431421` | 24 часа |
| `resolve:сбер` | `ИНН 7707083893` | 24 часа |

> 💡 **Экономия:** 30% повторяющихся запросов («Сбер», «Яндекс», «Магнит») обрабатываются из кэша без обращения к ФНС и тем более к LLM.

### Этап 3: Нормализация правилами (без LLM)

| Операция | Пример | Результат |
|----------|--------|-----------|
| Приведение к нижнему регистру | «ЯндКс» | «яндкс» |
| Удаление стоп-слов | «ООО Яндекс» | «яндекс» |
| Исправление очевидных опечаток | «яндкс» → «яндекс»<br>«сбрбанк» → «сбербанк» | Словарь из 50 частых опечаток |
| Удаление спецсимволов | «Яндекс!» | «яндекс» |

> ⚠️ **Важно:** Словарь опечаток формируется на основе логов реальных запросов пользователей (первые 2 недели работы сервиса).

### Этап 4: Поиск в ФНС с оценкой релевантности

Релевантность рассчитывается по формуле:
```
Релевантность = 0.6 × совпадение_по_словам + 0.3 × совпадение_по_региону + 0.1 × популярность_компании

Где:
• Совпадение по словам: расстояние Левенштейна между запросом и названием (нормализованное)
• Совпадение по региону: 1.0 если регион пользователя совпадает с регионом компании, иначе 0.5
• Популярность: оценка по количеству вакансий на hh.ru (если доступно)
```

**Пороги принятия решений:**
- Релевантность ≥0.85 → однозначное совпадение
- Релевантность 0.60–0.84 → возможна неоднозначность (требуется анализ количества результатов)
- Релевантность <0.60 → низкая уверенность

---

## 🌐 Этап 5: Внешний поиск (без LLM)

**Цель:** Найти ИНН/ОГРН через публичные сниппеты поисковых систем **без вызова LLM**.

**Алгоритм:**
1. Сформировать поисковый запрос: `"{нормализованный_запрос} официальный сайт ИНН"`
2. Выполнить поиск через Яндекс.XML API или парсинг результатов (с соблюдением `robots.txt`)
3. Проанализировать первые 5 сниппетов на наличие паттернов ИНН/ОГРН:
   ```regex
   \b\d{10}\b  # ИНН юрлица
   \b\d{12}\b  # ИНН ИП
   \b\d{13}\b  # ОГРН
   ```
4. Если найден ИНН с высокой уверенностью (встречается в ≥2 сниппетах) → использовать его для запроса к ФНС

**Пример:**
```
Запрос: "яндкс такси"
Поисковый запрос: "яндкс такси официальный сайт ИНН"
Сниппет 1: "Яндекс.Такси — заказ такси. ИНН 7704431421"
Сниппет 2: "Официальный сайт Яндекс.Такси (ИНН 7704431421)"
→ Найден ИНН 7704431421 → запрос к ФНС
```

> 💡 **Экономия:** 40% «сложных» запросов (с опечатками) разрешаются через внешний поиск без вызова LLM.

---

## 🤖 Этап 6–7: Минимально необходимый вызов DeepSeek

### Когда вызывать LLM (только эти случаи):

| Сценарий | Пример | Контекст для промпта |
|----------|--------|----------------------|
| **Неоднозначность 2–3 вариантов** | «Магнит» → ПАО Магнит / ИП Магнит | Передать 3 варианта из ФНС с ИНН и регионом |
| **Бренд ≠ юрлицо** | «Самокат» → ООО Умное пространство | Передать факт: «Самокат — бренд, принадлежащий ООО Умное пространство (ИНН 7811554010)» |
| **Опечатка + не найдено в сниппетах** | «сбр» → не найдено ИНН в поиске | Передать: «Пользователь ввёл "сбр". Возможные варианты: Сбербанк, Сберлогистика» |
| **Запрос на уточнение** | «Тинькофф» → банк или ресторан? | Сгенерировать 2–3 уточняющих вопроса для пользователя |

### Оптимизированный промпт для DeepSeek (минимизация токенов)

```text
Ты — эксперт по российским компаниям. Выбери компанию или предложи уточнение.

Запрос: "{запрос}"

Варианты из реестра:
1. "{название1}" (ИНН {инн1}, {регион1})
2. "{название2}" (ИНН {инн2}, {регион2})
{...максимум 3 варианта...}

Правила ответа:
- Если один вариант очевидно верный → верни его ИНН
- Если варианты равновероятны → предложи 1 уточняющий вопрос
- Ответ строго в JSON без пояснений

Формат:
{"action":"select|clarify","inn":"строка или null","clarify":"вопрос или null"}
```

**Пример ответа:**
```json
{"action":"select","inn":"7704431421","clarify":null}
```
или
```json
{"action":"clarify","inn":null,"clarify":"Вы имеете в виду банк Тинькофф (ИНН 7736207543) или ресторанную сеть Тинькофф?"}
```

> 💡 **Экономия токенов:**
> - Короткий системный промпт (80 токенов)
> - Максимум 3 варианта в контексте (120 токенов)
> - Требование строгого JSON (без «лирики»)
> - **Итого: ~200 токенов на вызов** вместо 500+ при неоптимизированном промпте

---

## 📈 Прогноз нагрузки на DeepSeek

| Тип запроса | Доля трафика | Обработка | Вызовы LLM |
|-------------|--------------|-----------|------------|
| ИНН/ОГРН | 25% | Прямой поиск в ФНС | 0% |
| Частые названия (Сбер, Яндекс) | 30% | Кэш + ФНС | 0% |
| Редкие названия с однозначным результатом | 25% | ФНС + внешний поиск | 0% |
| Неоднозначные названия | 15% | ФНС → внешний поиск → **LLM** | 100% |
| Сложные опечатки | 5% | Внешний поиск → **LLM** | 100% |
| **Итого** | **100%** | | **~5% запросов → вызов LLM** |

**Расчёт стоимости при 100 запросах/день:**
- Вызовы LLM: 5 шт.
- Токенов на вызов: 200
- Всего токенов/день: 1 000
- Всего токенов/месяц: 30 000
- **Бесплатная квота DeepSeek: 5 000 000 токенов → запас 166×**

---

## ⚙️ Техническая реализация кэширования

### Структура кэша (Redis)

```redis
# Нормализация запросов
SET normalize:яндкс такси "яндекс такси" EX 604800  # 7 дней

# Результаты идентификации
SET resolve:яндекс такси "7704431421" EX 86400     # 24 часа
SET resolve:сбер "7707083893" EX 86400

# Частые опечатки (формируется из логов)
SET typo:яндкс "яндекс"
SET typo:сбр "сбербанк"
```

### Алгоритм кэширования ответов LLM

1. После успешного вызова LLM сохранить маппинг:
   ```
   Ключ: "llm_resolve:{хэш(запрос + контекст)}"
   Значение: {"inn": "7704431421", "confidence": 0.95}
   Срок: 24 часа
   ```
2. При повторном запросе с идентичным контекстом — брать из кэша без вызова LLM
3. Инвалидировать кэш при изменении статуса компании в ФНС (ежедневная проверка)

---

## 🚨 Обработка крайних случаев без LLM

| Сценарий | Решение без LLM |
|----------|-----------------|
| **Пользователь ввёл ФИО руководителя** | Поиск в ФНС по ФИО → если найдено ≤3 компании → показать выбор пользователю |
| **Запрос на иностранную компанию** | Поиск в открытом реестре иностранных компаний → если не найдено → сообщить «Сервис поддерживает только РФ» |
| **Очевидная ошибка («компания 12345»)** | Правило: если запрос состоит только из цифр и <10 символов → запросить уточнение |
| **Запрос «не знаю ИНН»** | Показать подсказку: «Введите название или поищите ИНН на сайте компании в разделе „Реквизиты“» |

---

## ✅ Чек-лист для минимизации вызовов LLM

- [ ] Реализована классификация типа запроса через регулярные выражения (без LLM)
- [ ] Настроен кэш нормализованных запросов (срок 7 дней)
- [ ] Реализован словарь частых опечаток (50+ записей)
- [ ] Интегрирован внешний поиск через Яндекс с извлечением ИНН из сниппетов
- [ ] Настроен кэш результатов идентификации (срок 24 часа)
- [ ] Промпт для DeepSeek оптимизирован до ≤200 токенов
- [ ] Реализована инвалидация кэша при изменении статуса компании
- [ ] Добавлены правила для обработки крайних случаев (ФИО, иностранные компании)

---

## 📌 Рекомендация для старта с DeepSeek

1. **Первые 2 недели:** Работать без LLM вообще — только правила + ФНС + внешний поиск. Собирать логи необработанных запросов.
2. **Анализ логов:** Выделить 20 самых частых «сложных» запросов, для которых не сработали правила.
3. **Настройка словарей:** Дополнить словарь опечаток и нормализации на основе логов.
4. **Подключение DeepSeek:** Включить только для оставшихся 5% запросов с неоднозначностью.
5. **Мониторинг:** Отслеживать долю вызовов LLM — цель ≤5%.

> 💡 **Итог:** При правильной настройке каскадной стратегии **95% запросов обрабатываются без вызова LLM**, что делает использование бесплатной квоты DeepSeek полностью достаточным для старта сервиса.